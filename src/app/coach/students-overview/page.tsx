import StudentWorkspace from '@/components/student/StudentWorkspace';

export const dynamic = 'force-dynamic';

export default function StudentsOverviewPage() {
  return <StudentWorkspace mode="coach" />;
}
